<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
  </head>
  <body>
    <h2>Your Profile</h2>
    <p>Username: <%= user.username %></p>

    <% if (user.photo) { %>
    <p><img src="<%= user.photo %>" alt="Profile Picture" width="150" /></p>
    <% } else { %>
    <p>No profile photo uploaded.</p>
    <% } %>

    <p><a href="/profile/edit">Edit Profile</a></p>
  </body>
</html>
