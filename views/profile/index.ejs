<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/4.css" /> 
    <title>Profile</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %> <!-- Include Navbar -->

    <div class="profile-container">
      <h2>Your Profile</h2>

    
      <% if (user.photo) { %>
        <img src="<%= user.photo %>" alt="Profile Picture" class="profile-img" />
      <% } else { %>
        <p>No profile photo uploaded.</p>
      <% } %>

     
      <div class="profile-info">
        <p><strong>Username:</strong> <%= user.username %></p>
        <p><a href="/profile/edit" class="edit-link">Edit Profile</a></p>
      </div>
    </div>
  </body>
</html>

